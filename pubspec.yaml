name: multi_platform_example
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.1
  shake: ^2.0.0
  shared_preferences:
  panorama: ^0.4.1
  background_stt: ^1.0.6
  youtube_player_flutter:
  image_picker: ^0.7.2+1
  imageview360: ^1.3.0
  restart_app:
  splashscreen:
  flutter_barcode_scanner:
  speech_to_text:
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

flutter:

  uses-material-design: true
  assets:
    - assets/
    - assets/car_models/
    - assets/car_showroom/
    - assets/car-inside/
